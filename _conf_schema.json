{
  "max_budget": {
    "type": "int",
    "description": "Maximum battery budget per command",
    "hint": "Prevents oversized command input. Default: 300000.",
    "default": 300000
  },
  "daily_first_multiplier": {
    "type": "int",
    "description": "Daily first-gift multiplier used by automatic optimization",
    "hint": "Used when the command has no FirstGift1/2/3 option. Default: 3. The optimizer evaluates banner, captain, admiral, governor, and food plans.",
    "default": 3
  }
}
